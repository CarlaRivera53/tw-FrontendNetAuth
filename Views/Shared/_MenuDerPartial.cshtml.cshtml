<div class="navbar-nav small">
    <div class="navbar-text-from-check fro-switch from-check-reverse me-2">
        <input class ="from-check-input" type="checkbox" id="darkModelSwitch">
        <label class="from-check-label" for="darkModeSwicth"><i class="bi bi-moon-stars modo-luz"></i></label>
    </div>
    @if (User.Identity?.IsAuthenticated == true)
    {
        <a class="nav-link" asp-controller= "Perfil"> Hola @(User.Identity.Name)!</a>
        <a class="nav-link" asp-controller="Auth" asp-action= "Salir">Salir</a>
    }
    else{
        <a class="nav-link" asp-controller="Auth" asp-action="="Index>Iniciar Sesion</a>
    }
</div>